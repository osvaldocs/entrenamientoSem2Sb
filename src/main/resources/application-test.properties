# application-test.properties
spring.application.name=H1-Test

# Perfil activo (opcional si lo definís desde línea de comando)
spring.profiles.active=test

# Configuración para test in-memory
# Si tu repositorio in-memory necesita algún flag
app.repository.type=inmemory

# Otros valores de ejemplo para test
server.port=8081
logging.level.root=DEBUG
